# OpenAI API key for summarizing release notes (required)
# Sign up at https://platform.openai.com/ to get your API key
OPENAI_API_KEY=your_openai_api_key_here

# GitHub username (optional)
# This is used for reference when using the GitHub CLI
GITHUB_USERNAME=your_github_username_here

# Default repository to fetch releases from if none specified (optional)
# Uncomment and modify this if you want to change the default repository
# DEFAULT_REPO=owner/repository

# Note: Ensure you're authenticated with GitHub CLI before running the script
# Run 'gh auth login' if you haven't already done so 